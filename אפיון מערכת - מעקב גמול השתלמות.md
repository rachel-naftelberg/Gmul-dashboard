# אפיון מערכת: מעקב וניהול גמול השתלמות 🌿

מסמך אפיון קצר עבור מערכת **מעקב גמול השתלמות (Gmul-dashboard)**, המרכז את מטרות המערכת, היכולות הטכנולוגיות, הדרישות הפונקציונליות וחוקי הסקטורים המובנים.

---

## 1. מבוא ומטרת העל
מערכת מעקב גמול השתלמות נועדה לספק לעובדים כלי דיגיטלי עצמאי, פשוט ומאובטח לריכוז, חישוב וניהול שעות ההשתלמות והקורסים שלהם לצורך הגשת בקשות לגמול השתלמות (גמול א' וגמול ב'). המערכת מאפשרת למשתמש לראות בכל רגע נתון היכן הוא עומד ביחס לדרישות הסקטור המקצועי שלו.

---

## 2. עקרונות ארכיטקטורה וטכנולוגיה
* **Local-First (מקומית לחלוטין):** המערכת שומרת על פרטיות מידע מוחלטת. כל הנתונים, קובץ האקסל והמסמכים המצורפים מעובדים ונשמרים אך ורק במחשב ובדפדפן של המשתמש. אין שרת חיצוני ואין העלאת מידע לרשת.
* **טכנולוגיות ליבה:** HTML5, CSS3 (ונילה), JavaScript (ES6+).
* **ממשק קבצים מתקדם:** 
  - שימוש ב-**File System Access API** כדי לאפשר לדפדפן לקרוא ולכתוב ישירות לתיקייה מקומית במחשב (לאחר אישור הרשאה מהמשתמש).
  - ספריית **SheetJS (XLSX)** לקריאה ישירה של נתוני קובץ האקסל המקומי וכתיבה אליו בזמן אמת.

---

## 3. מבנה תיקיית העבודה (במחשב המשתמש)
בעת חיבור תיקיית העבודה, המערכת מייצרת ומנהלת את המבנה הבא:
* `מעקב גמול השתלמות.xlsx` – קובץ האקסל המרכז את כל שורות הקורסים והנתונים.
* `HELP.txt` – קובץ מדריך למשתמש מקומי ופשוט בפורמט טקסט.
* תיקיית `מסמכים/` – תיקיית משנה שבה המערכת שומרת ומאחסנת את הקבצים הסרוקים (PDF/תמונות) של תעודות הסיום, הסילבוס ואישורי משרד החינוך, ממוינים לפי שמות הקורסים.

---

## 4. פונקציונליות מרכזית
### א. מסך חיבור ראשוני (Setup)
* בחירת הסקטור המקצועי של המשתמש מתוך רשימה נפתלת.
* כפתור **"בחר תיקיית נתונים"** לקישור תיקיית המחשב.
* **זיהוי ויצירת אקסל חדש:** במידה ונבחרה תיקייה ריקה, המערכת מזהה זאת אוטומטית ומציעה לייצר קובץ אקסל חדש ונקי עם כל עמודות המעקב הנדרשות.

### ב. דאשבורד מעקב וסיכום שעות
* כרטיסיות סיכום המציגות: שעות שבוצעו, שעות בתהליך, ושעות חסרות להשגת היעד.
* מדדי התקדמות ויזואליים (Progress Bars) המציגים את אחוז ההתקדמות לעומת היעדים.
* סינון מהיר של הטבלה לפי סטטוס הקורסים (בוצע בלבד / בוצע + בתהליך).
* מעבר קל בין לשוניות גמול א' וגמול ב'.

### ג. טבלת ניהול קורסים
* תצוגה מורחבת (רוחב דף 1400px) המונעת גלישת שורות בתאי הטבלה (`white-space: nowrap`) לקריאות מושלמת.
* הוספת קורס מהירה דרך טופס מובנה, הכולל לחצן **"ניקוי"** לאיפוס מיידי של כל השדות.
* **עריכה רכה (Soft-Filtering):** שינוי ישיר של נתונים בתוך הטבלה (כמו סטטוס או סוג גמול) לא גורם להעלמה מיידית של השורה מהעין גם אם היא אינה מתאימה לסינון הפעיל באותו רגע, כדי לאפשר עבודה רציפה ונוחה.
* כפתור מחיקת קורס וכפתור **"שמור באקסל"** לכתיבה מהירה ושמירת כל השינויים.

### ד. ניהול מסמכים מצורפים
* תיבות סימון (Checkboxes) מובנות בטבלה לעדכון מיידי של קיום מסמכי חובה (תעודה, סילבוס, אישור יחידה/משרד החינוך).
* כפתור ניהול קבצים (📂) ליד כל קורס לפתיחת חלונית להעלאת קבצים סרוקים מקומיים והצגתם המהירה בדפדפן.

---

## 5. חוקים ומגבלות לפי סקטורים
המערכת מיישמת את הכללים והיעדים הנדרשים לכל סקטור:
1. **מהנדסים:**
   - יעד שעות כולל: **390 שעות** (לפחות 200 שעות מתוכן חייבות להיות מוגדרות כקורסי "הכשרה").
   - זכאים לגמול א' וגמול ב'.
   - כולל קישור ישיר למסמך ההסתדרות המקורי לצפייה ברשימת הקורסים המוכרים.
2. **אקדמאים והנדסאים:**
   - יעד שעות כולל: **400 שעות**.
   - זכאים לגמול א' וגמול ב'.
3. **מנהליים:**
   - יעד שעות כולל: **400 שעות**.
   - זכאים לגמול א' בלבד (לשונית גמול ב' מוסתרת אוטומטית בממשק).

---

## 6. עיצוב וחוויית משתמש (UI/UX)
* **קו עיצובי יוקרתי (Sage & Cream):** שימוש בצבעי ירוק מרווה ורקעי שמנת רכים בשילוב לוגו מותג SVG מונפש בעדינות.
* **אופטימיזציה למסכי לפטופ (14 אינץ'):** התאמת מרווחים (Margins, Padding, Gaps) כך שעמוד הכניסה הראשי מוצג במלואם על המסך ללא צורך בגלילה.
* **חלוניות נגררות (Draggable Modals):** כל החלוניות הקופצות (מדריך למשתמש, עריכת קורס וניהול מסמכים) ניתנות לגרירה חופשית באמצעות העכבר מעל כותרת החלונית.
